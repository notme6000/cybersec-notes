## EvilBox one (offsec)

1. did an nmap scan found 2 ports ssh and http in there default ports
2. did dir brute force using ffuf and found secret - dir and robots.txt
    - inside "Hello H4x0r"
    - also after doing another fuzzing inside secret with different file extensions we got a file "evil.php"
    - 



