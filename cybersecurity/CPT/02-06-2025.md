### nikto
- tool for finding web vulnerability
- `nikto -h <ip/domain name>`
### metasploit
- pentesting framework
- open source 
- provides payloads and other tools for hacking and pentesting
- `msfconsole`
### sqlmap
- tool for finding vulnerablity for sql-injection
- sql-injection uses backend sql queries for bypassing authentications
- sqlmap is used to find sql-injection vuln in a specific login-page
- `sqlmap -u <url of a specific login page>`
___
### sunset1 (ctf 2 flags)
1. find the sunset mechine in the same network using below device
	- netdiscover - passive scan for finding the devices in the same network
		- need root access
		- `netdiscover`
	- __arp-scan__ - active scan
		- `arp-scan -l`
2. find the ports in the device
	- using `nmap`
3. use nmap script or ftp login to search anonymous login and login to the system
	- `ftp <ip>`
4. download the backup file in the system useing
	- `get <filename>`
5. crack the hash
