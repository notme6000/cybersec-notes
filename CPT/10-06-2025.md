## dc-02
- nmap scan the ip (-A aggresive scan)
- the nmap scan will show http port 80 
	- the http page will no load
	- to load the page we need to add the device name and ip in `/etc/hosts`
	- after loading the page we can find the falg1 in the web-page
- cewl - password wordlist
	- the flag1 will give hint about the command to create password wordlist form the site `cewl <url> -w pass.txt`
	- after a passwordlist is created
- wpscan
	- wpscan is used for scanning the wordpress site for username and enumerate password for finding the correct password.
	- `wpscan --url <site-url> -p passwordlist.txt`
	- this will find the password.
- login to jerry
	- login using username jerry and the password found.
- nmap -p- scan
	- use `nmap -A -p- <ip> ` to find the common open ports.
	- there we will find the ssh port in 7744.
	- we can use the tom and password to connect via ssh to the system
- ssh
	- 